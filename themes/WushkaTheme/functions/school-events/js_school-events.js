jQuery(document).ready(function($) {
	/* -------------------------------
	 * 		Teacher Collections JS
	 * ------------------------------- */
	//console.log = function() {}

/*	function run_school_event_ajax(a_data) {
		$.ajax({
			url : temp_fle_drctry + 'ajax_school-events.php',
			type: 'post',
			dataType: 'json',
	        data: {
	        	'ajax_function' : 'save_event',
	        	'event_data' 	: a_data
	        },
	        error: function(){
	        	console.log('School Event Ajax Error: Ajax Failed to run');
	        },
	        success: function( ajax_return ) {
	        	if ( ajax_return === true ) {
	        		console.log('School Event Successfully Stored');
	        		return true;
	        	}
	        	console.log('School Event Failed to Save');
		        return false;
	        }
		});
	}*/

});